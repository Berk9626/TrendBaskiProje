@model List<ECommerce.WebUI.Dtos.AppUserDto.ResultAppUserListDto>
@{
    ViewData["Title"] = "UserList";
    Layout = "~/Views/AdminLayout/_AdminLayout.cshtml";
}



    <div class="col-lg-12">       
            
                <div class="active-member">
                    <div class="table-responsive">
                        <table class="table table-xs mb-0">
                            <thead>
                                <tr>
                                    <th>Kullanıcı</th>
                                    <th>Şehir</th>
                                    <th>Ülke</th>
                                    <th>Durum</th>
                                    <th>Cinsiyet</th>                                   
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                <tr>
                                    <td>@item.FirstName @item.LastName</td>
                                    
                                    <td>
                                        
                                    </td>
                                    <td>
                                        <div>
                                            <div class="progress" style="height: 6px">
                                                <div class="progress-bar bg-success" style="width: 50%"></div>
                                            </div>
                                        </div>
                                    </td>
                                    @* @if(item.Gender == "Erkek")
                                    {
                                        <td><i class="fa fa-circle-o text-success  mr-2"></i>@item.Gender</td>
                                    }
                                    @if (item.Gender == "Kadın")
                                    {
                                        <td><i class="fa fa-circle-o text-danger  mr-2"></i>@item.Gender</td>
                                    }  *@                                                           
                                </tr>  
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            
        
    </div>
<br />
<a href="/Register/Index" class="btn btn-primary">Yeni Kullanıcı Kaydı</a>
